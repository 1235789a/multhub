// Central contact configuration. Read from here so every entry point
// (ContactForm, policy pages, mailto links) stays in sync.
export const contactConfig = {
  email: "chengzhao640@gmail.com",
  replyWindow: "Replies within 2 business days",
} as const;
