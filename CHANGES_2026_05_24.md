# 2026-05-24 代码改动记录

## 主要变更

### 1. 产品价格统一为 USDT

**文件**: `src/app/data/products.ts`

**变更内容**:
- Tariff Lens · 关税透镜: `¥29 / 4 USDT` → `4 USDT`
- MarkItDown · 单页清洁工: `¥19.9` → `3 USDT`

**说明**: 将所有产品价格显示统一为纯 USDT 格式，保持内部字段不变。

---

### 2. 博客系统双语化改造

**修改文件**:
- `src/app/data/blog.ts` - 重构数据结构为双语格式
- `src/app/log/page.tsx` - 博客列表页支持语言切换
- `src/app/log/[slug]/BlogDetailClient.tsx` - 博客详情页支持语言切换

**新增文件**:
- `src/app/data/cms/README.md` - CMS 说明文档
- `src/app/data/cms/content/.gitkeep` - 内容目录占位

**数据结构变更**:
```typescript
// 旧结构
{ title: string; excerpt: string; body: string; }

// 新结构
{ title: { en: string; zh: string; }; excerpt: { en: string; zh: string; }; body: { en: string; zh: string; }; }
```

---

### 3. CMS 基础架构搭建

**目录结构**:
```
src/app/data/cms/
├── README.md          # CMS 使用说明
└── content/           # Markdown 内容目录
    └── .gitkeep       # 占位文件
```

---

## 架构说明

### 设计原则
- 保持现有代码结构完整性
- 数据与视图完全分离
- 完全向后兼容
- 易于后续扩展 CMS 功能

### 技术实现
- 使用现有的 `useLanguage()` hook 进行语言切换
- 数据通过 `lang` 变量动态选择 `en` 或 `zh` 版本
- 所有改动在 data 层完成，不影响核心组件逻辑

---

## Git 提交信息

```
refactor: 博客系统双语化 + 产品价格统一USDT

- 产品价格统一为 USDT 显示格式
- 博客数据结构升级为双语版本
- 博客列表页与详情页支持语言切换
- 搭建 CMS 基础架构框架
```

---

## 后续待办

- [ ] 完善 CMS Markdown 文件解析脚本
- [ ] 添加 `npm run build:blog` 脚本
- [ ] 实现从 Markdown 到 blog.ts 的自动生成
